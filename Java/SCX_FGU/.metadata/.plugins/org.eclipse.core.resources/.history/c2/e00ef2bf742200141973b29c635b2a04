package scx.fgu.graphics;

import java.util.Random;

public class Screen {

	private int width;
	private int height;
	public int[] pixels;
	public final int TILE_MAP_SIZE = 64;
	public final int TILE_MAP_SIZE_MASK = TILE_MAP_SIZE - 1;
	
	
	public int[] tiles = new int[TILE_MAP_SIZE * TILE_MAP_SIZE];
	
	private Random random = new Random();
	
	public Screen(int width, int height) {
		this.width = width;
		this.height = height;
		pixels = new int[width*height];
		
		for (int i = 0; i < TILE_MAP_SIZE * TILE_MAP_SIZE; i+=1) {
			tiles[i] = random.nextInt(0xFFFFFF);
		}
	}
	
	public void clear() {
		for (int i = 0; i < pixels.length; i++) {
			pixels[i] = 0;
		}
	}
	
	public void render(int xOffset, int yOffset) {
		for (int y = 0; y < height; y++) {
			int yShift = y + yOffset;
			//if(yShift < 0 || yShift >= height) break;
			for (int x = 0; x < width; x++) {
				int xShift = x + xOffset;
				//if (xShift < 0 || xShift >= width) break;
				int tileIndex = ((xShift >> 4) & TILE_MAP_SIZE_MASK) + ((yShift >> 4) & TILE_MAP_SIZE_MASK) * TILE_MAP_SIZE; // ">> 4" = "/ 16"
				pixels[x + y * width] = Sprite.grass.pixels[(x & 15) + (y & 15) * Sprite.grass.SIZE];
			}
		}
	}
	
}
